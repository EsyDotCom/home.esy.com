# Esy — Canonical Definition & Product Philosophy

> **Moved.** This document is now org-level canon and lives in **org.esy**:
> `apps/org-docs/brand/ESY_DEFINITION.md`.
>
> It was relocated because the brand definition binds all three Esy repos
> (`esy.com`, `app.esy.com`, `api.esy.com`), so it belongs in cross-repo canon
> rather than a single site's repo. Brand *assets* a site renders (logos, fonts)
> stay in their repo.
>
> Edit the canonical copy in org.esy; do not re-fork it here.
